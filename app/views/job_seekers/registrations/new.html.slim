h2
  | 求職者 サインアップ
= form_with model: @job_seeker, url: job_seeker_registration_path, local: true do |f|
  = devise_error_messages!
  .row
    .input-field
      = f.label 'メールアドレス'
      = f.email_field :email, autofocus: true, autocomplete: "email"
  .row
    .input-field
      = f.label 'パスワード'
      = f.password_field :password, autocomplete: "new-password"
      - if @minimum_password_length
        span.helper-text
          | *パスワードは
          = @minimum_password_length
          | 文字以上
  .row
    .input-field
      = f.label 'パスワード(確認)'
      = f.password_field :password_confirmation, autocomplete: "new-password"
  .row
    .actions
      = f.submit 'サインアップ', class: 'btn'
.row
  = link_to 'かんたんログイン', job_seekers_guest_sign_in_path, method: :post, class: 'btn'
= render "job_seekers/shared/links"
